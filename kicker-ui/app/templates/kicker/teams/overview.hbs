<table class="table table-condensed">
    <thead>
    <tr>
        <th>Team</th>
        <th>Aktionen</th>
    </tr>
    </thead>

    <tbody>
    {{#each teams as |team|}}
        <tr>
            <td>{{#link-to "kicker.teams.detail" team.id}}{{team.name}}{{/link-to}}</td>
            <td>
            </td>
        </tr>
    {{/each}}
    </tbody>

</table>
{{outlet}}
